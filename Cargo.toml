[package]
name = "keep_forward"
version = "0.1.1"
edition = "2024"

[dependencies]
rdev = "0.5"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.59", features = ["Win32_UI_Input_KeyboardAndMouse"] }
